## API Report File for "web3-validator"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts
/// <reference types="node" />

import { DataValidateFunction } from 'ajv/dist/types';
import { DataValidationCxt } from 'ajv/dist/types';
import { ErrorObject } from 'ajv';
import { JSONSchemaType } from 'ajv';

// @public (undocumented)
const abiSchemaToJsonSchema: (
	abis: ShortValidationSchema | FullValidationSchema,
	level?: number | undefined,
) => JsonSchema;

// @public (undocumented)
export type BlockNumberOrTag = string | BlockTag;

// @public (undocumented)
export type BlockTag = 'earliest' | 'latest' | 'pending';

// @public (undocumented)
export enum BlockTags {
	// (undocumented)
	EARLIEST = 'earliest',
	// (undocumented)
	LATEST = 'latest',
	// (undocumented)
	PENDING = 'pending',
}

// @public (undocumented)
export const checkAddressCheckSum: (data: string) => boolean;

// @public (undocumented)
const codePointToInt: (codePoint: number) => number;

export { DataValidateFunction };

export { DataValidationCxt };

// @public (undocumented)
const ethAbiToJsonSchema: (abis: ValidationSchemaInput) => JsonSchema;

// @public (undocumented)
export type EthBaseTypes = 'bool' | 'bytes' | 'string' | 'uint' | 'int' | 'address' | 'tuple';

// @public (undocumented)
export type EthBaseTypesWithMeta =
	| `string${string}`
	| `string${string}[${number}]`
	| `bytes${string}`
	| `bytes${string}[${number}]`
	| `address[${number}]`
	| `bool[${number}]`
	| `int${string}`
	| `int${string}[${number}]`
	| `uint${string}`
	| `uint${string}[${number}]`
	| `tuple[]`
	| `tuple[${number}]`;

// @public (undocumented)
export type EthExtendedTypes =
	| 'hex'
	| 'number'
	| 'blockNumber'
	| 'blockNumberOrTag'
	| 'filter'
	| 'bloom';

// @public (undocumented)
export interface Filter {
	// (undocumented)
	readonly address?: string | string[];
	// (undocumented)
	readonly fromBlock?: BlockNumberOrTag;
	// (undocumented)
	readonly toBlock?: BlockNumberOrTag;
	// (undocumented)
	readonly topics?: (string | string[] | null)[];
}

// Warning: (ae-forgotten-export) The symbol "AbiParameter" needs to be exported by the entry point index.d.ts
//
// @public (undocumented)
export type FullValidationSchema = ReadonlyArray<AbiParameter>;

// @public (undocumented)
const hexToNumber: (value: string) => bigint | number;

// @public (undocumented)
export const isAddress: (value: ValidInputTypes, checkChecksum?: boolean) => boolean;

// @public (undocumented)
export const isBigInt: (value: ValidInputTypes) => boolean;

// @public (undocumented)
export const isBlockNumber: (value: string) => boolean;

// @public (undocumented)
export const isBlockNumberOrTag: (value: string) => boolean;

// @public (undocumented)
export const isBlockTag: (value: string) => boolean;

// @public (undocumented)
export const isBloom: (bloom: ValidInputTypes) => boolean;

// @public (undocumented)
export const isBoolean: (value: ValidInputTypes) => boolean;

// @public (undocumented)
export const isBuffer: (data: ValidInputTypes) => boolean;

// @public (undocumented)
export const isBytes: (
	value: ValidInputTypes | Uint8Array | number[],
	options?:
		| {
				abiType: string;
				size?: never;
		  }
		| {
				size: number;
				abiType?: never;
		  },
) => boolean;

// @public (undocumented)
export const isContractAddressInBloom: (bloom: string, contractAddress: string) => boolean;

// @public (undocumented)
export const isFilterObject: (value: Filter) => boolean;

// @public (undocumented)
export const isHex: (hex: ValidInputTypes) => boolean;

// @public (undocumented)
export const isHexStrict: (hex: ValidInputTypes) => boolean;

// @public (undocumented)
export const isHexString32Bytes: (value: string, prefixed?: boolean) => boolean;

// @public (undocumented)
export const isHexString8Bytes: (value: string, prefixed?: boolean) => boolean;

// @public (undocumented)
export const isInBloom: (bloom: string, value: string | Uint8Array) => boolean;

// @public (undocumented)
export const isInt: (
	value: ValidInputTypes,
	options?:
		| {
				abiType: string;
				bitSize?: never;
		  }
		| {
				bitSize: number;
				abiType?: never;
		  },
) => boolean;

// @public (undocumented)
export const isNumber: (value: ValidInputTypes) => boolean;

// @public (undocumented)
export const isObject: (item: unknown) => item is Record<string, unknown>;

// @public (undocumented)
export const isString: (value: ValidInputTypes) => boolean;

// @public (undocumented)
export const isTopic: (topic: string) => boolean;

// @public (undocumented)
export const isTopicInBloom: (bloom: string, topic: string) => boolean;

// @public (undocumented)
export const isUInt: (
	value: ValidInputTypes,
	options?:
		| {
				abiType: string;
				bitSize?: never;
		  }
		| {
				bitSize: number;
				abiType?: never;
		  },
) => boolean;

// @public (undocumented)
export const isUserEthereumAddressInBloom: (bloom: string, ethereumAddress: string) => boolean;

// @public (undocumented)
export const isValidEthBaseType: (type: string) => boolean;

// @public (undocumented)
export type JsonSchema = Optional<JSONSchemaType<unknown>, 'type'> & {
	readonly eth?: string;
};

export { JSONSchemaType };

// @public (undocumented)
const numberToHex: (value: ValidInputTypes) => string;

// @public (undocumented)
export type Optional<T, K extends keyof T> = Pick<Partial<T>, K> & Omit<T, K>;

// @public (undocumented)
const padLeft: (value: ValidInputTypes, characterAmount: number, sign?: string) => string;

// @public (undocumented)
const parseBaseType: <T = string>(
	type: string,
) => {
	baseType?: T | undefined;
	baseTypeSize: number | undefined;
	arraySizes: number[];
	isArray: boolean;
};

// @public (undocumented)
export type ShortValidationSchema = ReadonlyArray<
	| string
	| EthBaseTypes
	| EthExtendedTypes
	| EthBaseTypesWithMeta
	| EthBaseTypesWithMeta
	| ShortValidationSchema
>;

declare namespace utils {
	export {
		parseBaseType,
		abiSchemaToJsonSchema,
		ethAbiToJsonSchema,
		codePointToInt,
		hexToNumber,
		numberToHex,
		padLeft,
	};
}
export { utils };

// @public (undocumented)
export const VALID_ETH_BASE_TYPES: string[];

// @public (undocumented)
export type ValidationSchemaInput = FullValidationSchema | ShortValidationSchema;

// @public (undocumented)
export const validator: Web3Validator;

// @public (undocumented)
export type ValidInputTypes = ArrayBuffer | Buffer | bigint | string | number | boolean;

// @public (undocumented)
export type Web3ValidationErrorObject = ErrorObject;

// @public (undocumented)
export type Web3ValidationOptions = {
	readonly silent: boolean;
};

// @public (undocumented)
export class Web3Validator {
	constructor();
	// (undocumented)
	validate(
		schema: ValidationSchemaInput,
		data: ReadonlyArray<unknown>,
		options?: Web3ValidationOptions,
	): Web3ValidationErrorObject[] | undefined;
	// (undocumented)
	validateJSONSchema(
		schema: object,
		data: object,
		options?: Web3ValidationOptions,
	): Web3ValidationErrorObject[] | undefined;
}

// @public (undocumented)
export class Web3ValidatorError extends Error {
	constructor(errors: Web3ValidationErrorObject[]);
	// (undocumented)
	readonly errors: Web3ValidationErrorObject[];
	// (undocumented)
	readonly name: string;
	// (undocumented)
	toJSON(): {
		name: string;
		message: string;
	};
}

// (No @packageDocumentation comment for this package)
```
